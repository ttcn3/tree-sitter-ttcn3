# NR5GC Corpus Baseline Metrics
#
# Recorded at v0.2.0 against references/code/NR5GC_IWD_26wk24/.
# These are the reference numbers — run.sh enforces regression thresholds
# with headroom above these values.
#
# Update this file when the grammar improves past the current thresholds
# (also bump MIN_CLEAN_FRACTION / MAX_ERROR_NODES in run.sh).

total_files = 385
clean       = 384
clean_pct   = 99.7
errors      = 2
error_files = 1

# Thresholds enforced by run.sh:
min_clean_pct   = 99.0   # floor (0.7% headroom over baseline 99.7%)
max_error_nodes = 10     # ceiling (8 nodes headroom over baseline 2)
